pipeline {
	agent any
	stages {
		stage('build') {
			steps {
				echo "build"
			}
		}
		stage('Test') {
			steps {
				echo "test"
			}
		}
		stage('integration stage') {
			steps {
				echo "integration test"
			}
		}

	}
}
